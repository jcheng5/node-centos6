name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: centos:centos6
    steps:
    - uses: actions/checkout@v2
    - uses: ./
    - name: Run a multi-line script
      run: |
        ls -l
